# YO Log PRO v19 — Dependente Python
# Instalare: pip install -r requirements.txt

# ── Optionale — sporesc functionalitatile ─────────────────────
pyserial>=3.5          # Control CAT radio serial (Yaesu/Icom/Kenwood/Elecraft)
requests>=2.28         # Callbook online (radioamator.ro, QRZ.com)
beautifulsoup4>=4.11   # Parsare HTML callbook

# ── Actualizare callbook ANCOM ─────────────────────────────────
pandas>=1.3            # Parsare fisiere XLSX ANCOM
openpyxl>=3.0          # Engine XLSX pentru pandas

# ── Build executabil (doar pe masina de build, nu la rulare) ──
# pyinstaller>=6.0

# ── Interfata web in Callbook (optional) ──────────────────────
# tkinterweb>=3.24     # Dezactiveaza daca da probleme la build
